meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt

run:
  persistent: true
  command: python main.py

containers:
  - name: data
    mount: /data